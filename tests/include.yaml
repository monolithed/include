# -*- coding: utf-8; indent-tabs-mode: nil; tab-width: 4; c-basic-offset: 4; -*-

default: &options
    out: ./files

include:
    out.txt:
        files:
            - files/one.txt
            - files/two.txt

        paths:
            - ./path1
            - ./path2
