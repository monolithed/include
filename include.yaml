# -*- coding: utf-8; indent-tabs-mode: nil; tab-width: 4; c-basic-offset: 4; -*-

default: &options
    route: /usr/local/www/build

include:
    build.css:
        <<   : *options
        files:
            - ../../file1.css
            - ../../file2.css

    build.js:
        <<   : *options
        files:
            - ../../file1.js
            - ../../file2.js
